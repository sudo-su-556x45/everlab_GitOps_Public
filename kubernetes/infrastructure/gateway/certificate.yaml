apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: everlab-wildcard
  namespace: gateway

spec:
  secretName: everlab-wildcard

  issuerRef:
    kind: ClusterIssuer
    name: everlab-ipa
    group: cert-manager.io

  dnsNames:
    - "*.prod.everlab.dev"
