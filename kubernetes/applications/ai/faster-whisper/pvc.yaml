apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: faster-whisper-models
  namespace: ai
  annotations:
    argocd.argoproj.io/sync-options: Prune=false,Delete=false

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: nfs

  resources:
    requests:
      storage: 10Gi
