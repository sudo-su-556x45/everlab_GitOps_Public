apiVersion: v1
kind: Service

metadata:
  name: piper
  namespace: ai

spec:

  selector:
    app.kubernetes.io/name: piper

  ports:

    - name: wyoming
      port: 10200
      targetPort: 10200

  type: ClusterIP
