apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nocodb-pg
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-10"

spec:
  project: default

  source:
    repoURL: https://github.com/sudo-su-556x45/everlab_GitOps_Public.git
    targetRevision: main
    path: kubernetes/applications/nocodb/postgres

  destination:
    server: https://kubernetes.default.svc
    namespace: nocodb

  syncPolicy:
    automated:
      enabled: true
      prune: false
      selfHeal: true

    syncOptions:
      - CreateNamespace=true

  revisionHistoryLimit: 5
