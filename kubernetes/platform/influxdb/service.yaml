apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: influxdb
  labels:
    app.kubernetes.io/name: influxdb
    app.kubernetes.io/part-of: everlab

spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: influxdb

  ports:
    - name: http
      port: 8086
      targetPort: http
